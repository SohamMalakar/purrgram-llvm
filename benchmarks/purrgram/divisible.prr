def count_divisible():
    count = 0
    for i in range(1, 1000000001):
        if i % 3 == 0 or i % 5 == 0:
            count += 1
    print(f"Count of numbers divisible by 3 or 5: {count}")

count_divisible()